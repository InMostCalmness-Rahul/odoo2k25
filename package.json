{
  "name": "odoo2k25-root",
  "private": true,
  "scripts": {
    "dev": "concurrently --raw \"npm run dev --prefix server\" \"npm run dev --prefix client\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
